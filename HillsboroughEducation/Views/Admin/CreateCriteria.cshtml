@model HillsboroughEducation.Models.Criteria

@{
    ViewBag.Title = "Create Criteria";
}

<h2>Create Criteria</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Criteria</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.DESCR)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.DESCR)
            @Html.ValidationMessageFor(model => model.DESCR)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Criteria", "Admin", "", "")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
